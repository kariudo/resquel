{
  "type": "mssql",
  "db": {
    "user": "sa",
    "password": "yourStrong(!)Password",
    "server": "localhost",
    "database": "test",
    "requestTimeout": "30000",
    "options": {
      "enableArithAbort": true
    }
  },
  "routes": [
    {
      "method": "get",
      "endpoint": "/customer",
      "query": "SELECT * FROM test.dbo.customers;"
    },
    {
      "method": "post",
      "endpoint": "/customer",
      "query": "INSERT INTO customers (firstName, lastName, email) VALUES ('{{ body.data.firstName }}', '{{ body.data.lastName }}', '{{ body.data.email }}'); SELECT * FROM customers WHERE id=SCOPE_IDENTITY();"
    },
    {
      "method": "get",
      "endpoint": "/customer/:id",
      "query": "SELECT * FROM test.dbo.customers WHERE id={{ params.id }}"
    },
    {
      "method": "put",
      "endpoint": "/customer/:id",
      "query": "UPDATE test.dbo.customers SET firstName='{{ body.data.firstName }}', lastName='{{ body.data.lastName }}', email='{{ body.data.email }}' WHERE id={{ params.id }}; SELECT * FROM test.dbo.customers WHERE id={{ params.id }}"
    },
    {
      "method": "delete",
      "endpoint": "/customer/:id",
      "query": "DELETE FROM test.dbo.customers WHERE id={{ params.id }}"
    }
  ]
}
